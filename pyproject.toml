[project]
name = "tradescouts-api"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
  "django>=4.2.24",
]

[build-system]
requires = ["uv_build>=0.8.17,<0.9.0"]
build-backend = "uv_build"

[tool.uv]
package = false
